# P13b — o cliente aprova a arte, e o orçamento visto pelo portal (api, worktree, ∥ P14)

Nota de desenho (preparação da Fase B, 23/09). **Worktree** `/tmp/claude-1000/-home-kennedy-Documents-repositories/b5b01fad-1ec2-4773-82be-b392a838e016/scratchpad/wt/p13b/api`, branch **`impl/p13b`**, criada da base **no commit zero do P14** (M3o-a promovida e o rename D-35 feito). Banco **próprio** `ankaa_implemento_p13b` e `node_modules` **próprio** (o P14 promove a M3o-b no fim do par). Preparação exata em `cruzamento-fase-b.md`. Plano: §5.1 (rotas de arte do portal, `GET /cliente/me/orcamentos/:id`), §6.3 (linhas de arte, resumo e leitura do orçamento), §7.1–§7.7, D-09, D-35 (já feito no commit zero), DD5; §8 G17 (lado API); §9 linha P13b.

## O que mudou em relação ao §9 original (Revisão 3.1)

O rename `PRE_APPROVE → APPROVE_VALUE` (e rota, `canApproveValue`, `waitingOnMe.valueApproval`) já está feito pelo commit zero do P14. Em troca, **`portal-read.service.ts` e `portal-projection.service.ts` inteiros são do P13b** neste par: entram aqui também `signatureFacts` lendo o eixo `signatureStatus` e o marco "Arte aprovada", que o §6.3 punha no P14. O `emission` do portal ("Para emitir falta…") **não** é deste pacote: depende do `assertEmissionReady` do P14 e entra na integração do par.

## Escopo exato

1. **Capacidade** `APPROVE_ARTWORK` em `portal-capabilities.ts` para MARKETING, COMMERCIAL, SELLER, REPRESENTATIVE, COORDINATOR (**não** PURCHASING; D-09/DD5; o `Record<ResponsibleRole,…>` obriga os 9 papéis) e `SECTION_IMPLIED_BY_CAPABILITY[APPROVE_ARTWORK] = ['VEHICLE','LAYOUT']`; o comentário de `:129` passa a citar `order-number-gate.ts` (DD12).
2. **Rotas** `portal-artwork.controller.ts` + `portal-artwork.service.ts` + `src/schemas/portal-artwork.ts`: `GET /cliente/me/artes?status=PENDING_APPROVAL`, `PUT /cliente/me/veiculos/:taskId/artes/:layoutId/aprovar`, `…/reprovar { motivo }` (obrigatório), `PUT /cliente/me/artes/aprovar { layoutIds[] }` (lote **atômico**). Escopo `commercialTaskScopeWhere`: **404** fora (nunca 403); **409** "Esta arte já foi decidida". Chamam `ImplementLayoutService.approveFromPortal/reproveFromPortal` (do P12; o P13b **não** o edita) com ator `RESPONSIBLE` e sessão. Registro no módulo do portal (`portal.module.ts`/`portal-read.module.ts`).
3. **Leitura** (`portal-read.service.ts`, `portal-projection.service.ts`): artes do veículo com `status IN (PENDING_APPROVAL, APPROVED, REPROVED)` + a última `LayoutDecision` (`artworks: [{ id, file, status, version, decidedAt, decidedBy{name}, canDecide, note }]`; `DRAFT`/`SUPERSEDED` nunca aparecem); a arte do orçamento pelos implementos, agrupável por arquivo ("Aprovar para os N veículos"); no orçamento: `valueApproval` (a `BudgetValueApproval` vigente), `signatureStatus` com rótulo (inclusive "Assinada fora do sistema"), `artwork { total, approved, awaitingCustomer, awaitingMe, atAnkaa }`; resumo `/cliente/me/resumo` com "artes aguardando você" e `valueApproval` (IN_NEGOTIATION ∧ `APPROVE_VALUE`); `byStatus` sem `PRE_APPROVED` (derivado do enum); `signatureFacts` lendo o eixo; marco "Arte aprovada". Toda chave nova entra **no `select` enumerado** (armadilha "select explícito descarta chave nova").
4. **Notificação ao contato**: `portal-notification.service.ts` dispara `layout.portal_pending_approval` (chave criada no seed pelo P12) quando a arte vai a `PENDING_APPROVAL`; nenhum id de contato em FK de `User`.
5. **Demo e contrato**: `scripts/seed-portal-demo.ts` com arte pendente e valor para aprovar; `docs/PORTAL-CONTRATO.md` com as rotas e campos novos (o espelho `web/src/api-client/portal.ts` casa campo a campo no P23), mais o trecho que o P14 deixar na nota dele.

## Arquivos que o P13b possui neste par

`src/modules/people/portal/{portal-capabilities,portal-read.service,portal-read.controller,portal-read.module,portal-projection.service,portal.module}.ts`, `portal-artwork.*` (novos), `src/schemas/portal-artwork.ts` (novo), `src/modules/common/notification/portal-notification.service.ts`, `scripts/seed-portal-demo.ts`, `tests/{portal-recorte,portal-cliente-boot}.test.ts`, `tests/portal-arte.test.ts` (novo), `docs/PORTAL-CONTRATO.md`, `docs/PORTAL-DO-RESPONSAVEL.md`. Gerados que ele pode regerar no próprio ramo se a régua pedir: `contracts/{labels,enums}.json` (a integração regera de novo).

## O que NÃO toca

`prisma/**`, `src/constants/**`, `portal-decision*.ts`, `src/modules/common/signature/**`, `src/modules/production/**` (inclusive o `ImplementLayoutService`), `src/modules/common/file/**`, `prisma/scripts/seed-notification-configs.ts`, `package.json`, `scripts/pre-deploy.sh`, `.residual-baseline.json`, `tests/portal-identificacao.test.ts`, `tests/e2e-portal/**`; web e app. Precisa de algo do P14 (tipo, função)? Não espere: registre em "Resultado" e o integrador liga depois do merge.

## Testes que o provam

Novo `test:portal-arte` (tabela-verdade das capacidades × 9 papéis; 404 fora do escopo; reprovar sem motivo → 400; lote atômico — um fora do escopo derruba o lote todo; 409 em decisão repetida; a aprovação fecha as O.S. e recalcula a tarefa pelo serviço do P12; ator `RESPONSIBLE` nunca em FK de `User`; G17 lado API: aprovação do portal + gravação interna antiga não desfaz a arte); `portal-recorte` com as colunas novas (arte, `valueApproval`, `signatureStatus`, `artwork`) respeitando o recorte; `test:portal-cliente:boot` com as rotas novas; a régua **no worktree** verde.
