# P11b — frente, porta traseira e projeto do implemento (api, sozinho)

Nota de desenho (preparação da Fase B, 23/09). Checkout principal, commits na base, depois do P11a. Plano: D-05, D-06 (DD4), D-07, D-11 (tabela), §5.1 (`/implement-measure/…/:side`, `PUT /implements/:id/project-files`, `layout-dimensions`), §5.2 (multipart `frontSide`), §6.1 (linhas de frente/porta), §6.3 (leitura do portal), §7.4; §8 G12, G15, G16; §9 linha P11b.

## Escopo exato

1. **Promove M2** (`20260924120100_implemento_frente_e_porta_traseira`): `frontSideMeasureId` + FK + índice; `rearDoorLeaves RearDoorLeaves?`, `rearDoorBarCount Int?` (CHECK `IN (2,3,4)`), `rearDoorHatchCount Int?` (CHECK `BETWEEN 0 AND 6`), sem regra cruzada (DD4); descompartilha as medidas (D-07); **cria `_IMPLEMENT_PROJECT_FILES`** (movido da M3 na Revisão 3.1). Blocos do alvo para `schema.prisma`; CHECKs em `objetos-pos-push.sql`.
2. **4 faces** no writer do P04: `IMPLEMENT_FACES = ['left','right','back','front']` em `src/constants/implement-faces.ts`, `FACE_FK`/`FACE_REL` com `front` (o `satisfies Record<ImplementFace, …>` do P04 faz o `tsc` apontar cada mapa); foto opcional na frente (`implement-measure.service.ts:451`); multipart `implementMeasurePhotos.frontSide` nos **3** `FileFieldsInterceptor` da tarefa; `side` validado em runtime (`z.enum` com as 4, G12) + `ParseUUIDPipe`; rótulo "Frente" e SVG com frente e porta; `IMPLEMENT_FACES` entra no G5 (`export-contracts.ts`, era "no P11 com `'front'`").
3. **Porta traseira**: zod (`z.nativeEnum(RearDoorLeaves)`, `2|3|4`, `0..6`) no `taskImplementSchema`/`implementUpdateSchema`; tracker (`rearDoorLeaves/BarCount/HatchCount`) com o mapa explícito campo → chave de notificação (G9); tradução de valor em `notification-dispatch.service.ts:2633`; `REAR_DOOR_LEAVES` (espelho TS) + rótulos "Bipartida/Tripartida" em `enum-labels.ts`.
4. **Cotador**: `FRENTE` em `layout-dimensions.service.ts` **só para app com versão ≥ a do P24** (o 1.4.1 desenharia a frente como "Motorista", risco 27; ler `req.appVersion`); bench `scripts/check-layout-dimensions.ts` antes e depois.
5. **Projeto do implemento**: `PUT /implements/:id/project-files { fileIds[] }` + multipart `implementProjectFiles`; contexto `implementProjectFiles` → `Clientes/{cliente}/Projetos/` em `files-storage.service.ts`, `contracts/file-contexts.json`, e `'_IMPLEMENT_PROJECT_FILES.A'` em `file-reference.service.ts` (G10). Os **dados** (os 4 PDFs e a cópia de `baseFiles`) chegam com a M3 (P12).
6. **Leitura do portal** (tirada do P13a na Revisão 3.1, para o par [P12 ∥ P13a] ficar disjunto): `TASK_BASE_SELECT` (`portal-read.service.ts:202-229`) com `implement.select { type, category, 4 medidas, rearDoor*, projectFiles }` e o bloco `implement` da projeção (§7.4: `measures {left,right,back,front}` em metros, `rearDoor {leaves, barCount, hatchCount} | null`, `projectFiles`; chave `back`, nunca `rear`) em `portal-projection.service.ts`. **A escrita** pelo portal é do P13a.

## Arquivos que o P11b possui (fase sozinha)

`src/constants/implement-faces.ts`, `implement-measure-writer.ts`, `implement-measure.service.ts`/controller/repository, `task.controller.ts` (interceptors), `schemas/{task,implement,implement-measure}.ts` (porta, `side`), `task-field-tracker.service.ts`, `notification-dispatch.service.ts` (tradução), `layout-dimensions.*`, `files-storage.service.ts`, `file-reference.service.ts`, os dois arquivos de leitura do portal (blocos acima), `enum-labels.ts`/`enums.ts` (porta), `scripts/export-contracts.ts` (faces), `contracts/**` regerado + cópias no web e no app, `prisma/schema.prisma` + a pasta promovida, `package.json`/`pre-deploy.sh` (bloco P11b).

## O que NÃO toca

`portal-identity.*`, `portal-request.*`, `schemas/portal-request.ts`, `schemas/portal-vehicle-identity.ts`, `portal-frozen-document.ts` (escrita do portal: P13a); arte, `Layout` (P12); orçamento (P14).

## Testes que o provam

G15 com **4 faces** (10 escritores × 4 × {criar, atualizar, apagar, foto}); G16 (FormData real do web × interceptors, com `frontSide`); G12 (`side` inválido → 400, nunca 500); "editar a medida de A não altera B" depois do descompartilhamento; porta fora da faixa → 400 (zod) e CHECK no banco (inserção direta falha); bench do cotador sem regressão nas 3 faces e com a 4ª só para app novo; `portal-recorte` (o bloco `implement` respeita o recorte `VEHICLE`); G10 com o contexto novo; G5 com as faces; ensaio da R-B verde com M1/M1s/M2 aplicadas; régua inteira.
