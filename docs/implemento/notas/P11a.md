# P11a — Implement no lugar de Truck, janela bilíngue e a série no implemento (api, sozinho)

Nota de desenho (preparação da Fase B, 23/09). Checkout principal, commits na base. Plano: §5.2–§5.5, §6.1 (sem frente/porta), §6.13 (linhas R-B), D-01..D-04, D-17, D-32, DD1; §8 G2, G7, G19, G20, G22, G23, G24, G32; §9 linha P11a. É o maior pacote da fase: commits pequenos por tema, régua ao fim.

## Escopo exato

1. **Promove M1 + M1s** (protocolo do §4.1): `git mv prisma/staged/r-b/20260924120000_truck_vira_implement prisma/migrations/` (idem `…120050_serie_no_implemento…`), copia do `schema.alvo.prisma` os blocos marcados nas duas fatias (model `Implement`, relação `Task.implement`, `type`, `ImplementCategory`, `serialNumber` + `serialNumberNormalized`, `Task.serialNumber` sem `@unique`, `spot` sem default), funde o bloco da M1s de `objetos-pos-push.r-b.sql` em `prisma/sql/objetos-pos-push.sql`, `migrate deploy` + `generate`, **no mesmo commit** em que o código compila de novo.
2. **Rename por AST, arquivo a arquivo** (nunca `sed` global): §6.1 "Modelo e tipos", "Rotas…", "Schemas zod", "Constantes", "Leitores de truck" — `types/implement.ts` derivado do Prisma (apaga `types/truck.ts`), módulo `src/modules/production/implement/` com `@Controller('implements')` + `TruckAliasController` fino (`Deprecation: true` + contador), `implementUpdateSchema.strict()` **sem série** (400 "a série se edita na tarefa"), `omit.implement`, `paint.service.ts` (SQL cru `"Implement"`, e o `catch` para de devolver `[]`) e `file-reference.service.ts` (`'Implement.vinPlateId'`) **no mesmo commit da promoção**, whitelist com `implement` **mantendo** `truck` e `layouts`, `TASK_FIELD_DOMAINS.implement` junto com o zod (`implement.serialNumber` exige também `identity`), `IMPLEMENT_CATEGORY` + alias `TRUCK_CATEGORY` no espelho TS (o contrato do G5 **mantém** a chave `TRUCK_CATEGORY`, que o teste do web lê), contexto de arquivo `implementVinPlate` (alias de `truckVinPlate`) em `files-storage.service.ts` e `contracts/file-contexts.json`.
3. **Janela bilíngue**: `translateLegacyImplementKeys` antes do zod (corpo, include, where, orderBy; `truck` e `implement` juntos → 400); `ImplementLegacyMirrorInterceptor` (quem pediu `truck` recebe `truck` **igual e profundo** a `implement`, F9); `DEPRECATED_QUERY_KEYS` (`Task.truck → implement`, contador por rota × versão, lendo `req.appVersion` do P06).
4. **Série (DD1) — os seis escritores**, inclusive os dois do portal, **porque o gatilho da M1s recusaria o W4/W5 até o P13a** (desvio do §6.13, que os punha no P13a): W1/W2 (repositório: `implement: { create: {…, serialNumber, spot} }` **sempre**; `update`), W3 (faixa), W4 (`portal-request.service.ts` `criarVeiculo`, `spot: null`), W5 (`portal-identity.service.ts` `gravar`: `tx.implement.update`, auditoria continua `TASK/serialNumber`), W6 (rollback com unicidade antes); as 4 unicidades (`validateTask`, `garantirUnicidade` do portal, `portal-request` `:772-795`, P2002); semântica (`truck/implement: null` → 400; ramos "cria se não existir" apagados, inclusive `portal-identity.service.ts:545-570`); tracker e changelog emitindo `field = 'serialNumber'`; `build()` do snapshot lendo `implement.serialNumber`/`implement.type` e **escrevendo as chaves velhas**; `quote-text.ts`, `dossier-assembler.service.ts`; comentário do `dps.builder.ts:522-524`. Leitores ficam no espelho (P28).
5. **G24 liga**: `.serial-allowlist` + base da catraca.

## Arquivos que o P11a possui (fase sozinha: pode tocar qualquer arquivo da api)

Os de §6.1 marcados P11 menos frente/porta; `prisma/schema.prisma`, as duas pastas promovidas, `prisma/sql/objetos-pos-push.sql`; nos arquivos do portal, **só** os blocos de série, de unicidade e o rename mecânico (`portal-identity.service.ts`, `portal-request.service.ts`, `portal-read.service.ts`, `portal-projection.service.ts` — as chaves de saída do portal não mudam aqui); `contracts/{labels,enums,file-contexts}.json` regerados + as cópias do web e do app (commit "contrato gerado" em cada repo irmão; conferir `pnpm exec vitest run src/constants/document-labels.test.ts` no web e `flutter test test/generated` no app); `package.json`/`pre-deploy.sh` (bloco P11a).

## O que NÃO toca

Frente, porta traseira, `IMPLEMENT_FACES` com `front`, M2 (P11b); qualquer coisa de arte, `Layout`, `Budget.layoutFiles`, `task.layouts` (P12 — na janela, `layouts` continua na whitelist e o include legado segue funcionando pelo M2M que ainda existe); estados do orçamento (P14); a escrita de frente/porta no portal (P13a).

## Testes que o provam

G2, G7 (setor × corpo real, nunca só ADMIN), G19 (W1–W6 + rollback), G20 (C1–C7 com o gatilho diferido **no banco da Fase B, que tem migrações**), G22, G23 (corpo do app 1.4.1), G24, G32 (URL exata da Agenda → 200 e ordenada), G4 com as fixtures dos três clientes (**nenhum 403/500**), G11 verde (série lida do implemento, hash igual), `tests/implement-legacy-compat.test.ts` (novo: `truck{…implementType}` grava `implement.type`; include `truck` devolve os dois iguais; os dois juntos → 400; `/trucks/batch-update-spots` e `?truckId=` funcionam), busca de tinta por placa e por série, `portal-requisicao` e `portal-identificacao` verdes (série cai no implemento com `field: 'serialNumber'`), ensaio da R-B verde com M1/M1s já aplicadas; régua inteira.
