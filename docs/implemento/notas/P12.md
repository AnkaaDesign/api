# P12 — arte do implemento, R1 e a arte na assinatura (api, checkout principal, ∥ P13a)

Nota de desenho (preparação da Fase B, 23/09). **Checkout principal**, commits na base, em paralelo com o P13a (worktree `impl/p13a`). Plano: §2A.7 (E2 só lido aqui), §2A.9, §5.1 (rotas de arte), §5.2 (arte na tarefa), §5.4 (`task.layouts` sintetizado), §6.2 inteiro (menos os itens do P14), §6.12, §7.1, D-08, D-10, D-13..D-15, D-20, D-21, D-31 (lado arte), DD3, DD6, DD9, **DD10 (confirmada)**; §8 G11, G33; §9 linha P12. Divisão de arquivos com o P13a: `cruzamento-fase-b.md`.

## Escopo exato

1. **Promove M3** (`20260924120200_arte_do_implemento_e_projeto_da_tarefa`): `Layout.implementId`, estados, `LayoutDecision`, fan-out, arte do orçamento com `File.id` preservado (M-A..M-D; lê `BudgetLayoutTask` em `PER_VEHICLE`), PDFs → `Task.projectFiles`, dados do projeto do implemento, O.S. em `WAITING_APPROVE`, `DROP "_TaskLayouts"`. No `schema.prisma`: `File.layouts` → `File.artLayouts Layout[]`, sai `Task.layouts`, e **saem do Prisma** `Budget.layoutFiles`/`File.quoteLayoutId`/`QUOTE_LAYOUT` (a coluna fica no banco até a M4) com a entrada de saída de `file-reference.service.ts` e a guarda de lista vazia **no mesmo commit** (risco 4). Espelhos TS e rótulos: `LAYOUT_STATUS` com 5 ("Rascunho", "Aguardando aprovação do cliente", "Aprovada", "Reprovada", "Substituída"), `LAYOUT_APPROVAL_SOURCE`.
2. **`ImplementLayoutService`** (em `src/modules/production/implement/`, exportado pelo módulo para o P13b): `upload` (sempre `DRAFT`, só imagem; PDF → 400 "PDF cotado vai em Projeto da tarefa"), `send`, `approveOnBehalf` (COMMERCIAL/ADMIN, **nota obrigatória**), `reprove`, `newVersion` (`supersedesId`; a anterior vira `SUPERSEDED` só quando a nova é aprovada), `bulk`, **`approveFromPortal`/`reproveFromPortal`** (ator `RESPONSIBLE`, `fileSha256`, `updateMany … where status` contra a corrida G17); cada decisão grava `LayoutDecision`, chama `onQuoteContentChanged` do orçamento da tarefa (D-31: `RUNNING` invalida; `COMPLETED` não, com `tolerateArtworkAfterSeal` e deriva) e **recalcula o estado da tarefa** na mesma transação. Rotas do §5.1 (`/implements/:id/layouts/*`, `/implements/layouts/bulk`). Eventos com ator discriminado (`{ kind: 'USER' | 'RESPONSIBLE', id, name }`; nunca id de contato em FK de `User`).
3. **DD3 + DD10**: argumento `artworkGate: 'OK' | 'PENDING' | 'NOT_APPLICABLE'` em `calculateCorrectTaskStatus` e `getTaskUpdateForServiceOrderStatusChange` (`src/utils/task-service-order-sync.ts`), calculado pelos 6 chamadores (`task.service.ts`, `service-order.service.ts`); **a liberação manual "Disponibilizar para produção"** (`task.service.ts` ~`:4370` e `service-order.service.ts` ~`:926`, hoje "bypasses it") **também respeita `'PENDING'`** — DD10 confirmada; "só trabalho novo" = primeira O.S. de ARTE criada depois da R-B. Aprovar a arte fecha a O.S. "Aprovar com o Cliente" (descrição normalizada `lower(unaccent)`) **e** a O.S. de ARTE em `WAITING_APPROVE`; reprovar as devolve a `IN_PROGRESS`. Sem dispensa (DD9).
4. **R1 inteiro** (§6.2 itens 1–32, menos M4, `budget.guards.ts:215` e os itens 17-parte-valor/18/32, que são do P14): `sync-quote-task-layouts.ts` apagado; escritores de `layoutFileIds`/`layouts` do orçamento (inclusive os da `main`, item 26b) viram "aceita e ignora com log"; `quoteLayoutFile` → 400; `findPublic` devolve `artwork` pelo `select`; include padrão da tarefa sem `quote.layoutFiles` (**senão toda leitura de tarefa dá 500**); `layoutGateFailure` fala da arte do implemento; `budgetApprove` perde o portão de layout (o resto do "aprovar valor" é do P14).
5. **Assinatura**: `src/utils/quote-artwork.ts` (`quoteArtworkOf`: todas as tarefas, canceladas inclusive, ordem canônica, `unique(fileIds).sort()`); snapshot v4/material v7 **sem versão nova**; `layoutCoverage` só quando não uniforme **ou** `layoutScope = PER_VEHICLE`; documento com legenda por veículo (`quote-html.builder.ts`, `quote-renderer.service.ts`, `signature-envelope.service.ts:2464-2567`); diff "Arte aprovada".
6. **Compatibilidade**: sintetizador de `task.layouts` (`task-prisma.repository.ts:810-830`) sobre `implement.layouts` + `projectFiles` (`PENDING_APPROVAL`/`SUPERSEDED` aparecem como `DRAFT`); `layoutIds` no-op tolerante / 400 se muda; filtro por papel da PRODUÇÃO sobre `implement.layouts` (risco 25); `airbrushing.service.ts:1040` restrito a `{ fileId, airbrushingId }` (risco 29); `findUnique({ where: { fileId } })` → `findFirst` com dono.
7. **Portal, só a fonte da arte** (neste par, `portal-read.service.ts` e `portal-projection.service.ts` são do P12): o detalhe do orçamento e a projeção trocam `Budget.layoutFiles` por `quoteArtworkOf` (`:1048,1115-1117`; `:233,576,790-791`) e a arte do veículo troca `task.layouts` por `implement.layouts` **com o mesmo filtro `APPROVED`** (`:1212-1222`; `:878-891`). Comportamento de tela igual; as artes pendentes para aprovar são do P13b.
8. **Notificações** (seed): `layout.portal_pending_approval` e o lembrete do contato (o P13b só os usa); lembrete diário lendo `PENDING_APPROVAL` > 24 h (`task-notification.scheduler.ts`), com o método de destinatário em `portal-notification.service.ts` (do P12 neste par).

## Arquivos que o P12 possui neste par

`prisma/schema.prisma` + a pasta promovida; `src/modules/production/implement/**` (layout service/controller), `task.service.ts`, `task.controller.ts`, `task-prisma.repository.ts`, `task-notification.scheduler.ts`, `layout.events.ts`, `layout.listener.ts`, `service-order.*`, `src/utils/{task-service-order-sync,quote-artwork,quote-layout-coverage,sync-quote-task-layouts}.ts`, `budget.service.ts`, `budget-prisma.repository.ts`, `schemas/{budget,task,task-bulk}.ts`, `src/types/{budget,task,file}.ts`, `airbrushing.service.ts`, `src/modules/common/file/**` (inclusive `file-reference.service.ts`, `files-storage.service.ts`), `contracts/file-contexts.json`, `src/modules/common/signature/**` (snapshot, diff, documento, `signature-envelope.service.ts`), `portal-read.service.ts`, `portal-projection.service.ts`, `portal-notification.service.ts`, `notification-template.service.ts`, `prisma/scripts/seed-notification-configs.ts`, `src/constants/{enums,enum-labels}.ts`, `contracts/{labels,enums}.json` + cópias no web/app, `contracts/signature-golden/**`, `tests/{signature-golden-hashes,quote-diff,quote-merge-rules,signature-refusal,layout-per-vehicle,portal-recorte}.test.ts`, `tests/implement-layout.test.ts` (novo), `package.json`, `scripts/pre-deploy.sh`, `.residual-baseline.json`.

## O que NÃO toca

`portal-identity.*`, `portal-request.*`, `schemas/portal-request.ts`, `schemas/portal-vehicle-identity.ts`, `portal-vehicle-identity.ts`, `portal-frozen-document.ts`, `tests/{portal-identificacao,portal-requisicao,portal-cliente-boot}.test.ts`, `tests/e2e-portal/**`, `docs/PORTAL-CONTRATO.md` (P13a; o trecho do contrato sobre a arte do orçamento vai na seção "Resultado" desta nota e o integrador aplica). Estados do orçamento, `signatureStatus`, emissão, cobrança (P14).

## Testes que o provam

**G11 verde: quem casava antes casa depois** (código novo + M3 aplicada; casos sintéticos 1 veículo, N iguais, N diferentes, `PER_VEHICLE` uniforme e não uniforme, o nº 594 com tarefas canceladas); `quote-diff` (coverage condicional), `quote-merge-rules`, `signature-refusal`, `layout-per-vehicle` reescrito para a arte do implemento; `tests/implement-layout.test.ts`: send / approve-on-behalf sem nota → 400 / reprove / new-version / bulk atômico, fechamento das O.S. (normalizada; `WAITING_APPROVE`), **liberação automática e manual só com arte, só trabalho novo** (tabela das duas funções puras × 3 valores), ator discriminado, G33 (lado arte); exclusão de arquivo funciona (G10, risco 4); include `layouts` do app velho → sintetizado (G4); ensaio da R-B com M3 aplicada; régua inteira.
