# P05 — Rótulos com perfil por documento e contratos exportados (api + web + app, ∥ P04)

Nota de desenho (P00, 23/09). Plano: §9 linha P05, D-18, §8 G5 e G21, §6.13 (fiscais). **Onde roda:**
- **api** num **worktree próprio**: `/tmp/claude-1000/-home-kennedy-Documents-repositories/b5b01fad-1ec2-4773-82be-b392a838e016/scratchpad/wt/p05/api`, branch **`impl/p05-rotulos`**, criada de `feat/portal-do-responsavel` no mesmo commit em que o P04 começa (depois do P01). `node_modules` por link simbólico para o do checkout principal (o P05 não muda dependência nem schema Prisma; o cliente gerado serve). Integração: merge de `impl/p05-rotulos` na base depois do P04 (sem conflito se a divisão de donos for respeitada).
- **web** e **app** no checkout principal (`web` em `feat/portal-do-responsavel`; `mobile-flutter` em `feat/implemento`), depois do P03 e do P02.

## Escopo exato

1. **Fonte única de rótulos com perfil por documento (D-18, DD5).** Hoje os documentos **divergem**: NFS-e da tarefa diz "Isotérmico"/"Prancha/Plataforma" (`elotech-oxy-nfse.service.ts:1311`); boleto, fatura e tela dizem "Isoplastic"/"Carroceria" (`sicredi-boleto.scheduler.ts:1022`, `invoice-generation.service.ts:1419`, `src/constants/enum-labels.ts:409`); a NFS-e do pintor usa os rótulos **de tela** importados de `enum-labels.ts` (`dps.builder.ts:514-520`: `TRUCK_CATEGORY_LABELS`/`IMPLEMENT_TYPE_LABELS`), então mexer no mapa de tela muda a nota do pintor. Criar `src/constants/document-labels.ts` com **um perfil por documento** (`screen`, `nfseTask`, `nfsePainter`, `boleto`, `invoice`) que reproduz **byte a byte** o texto de hoje, e fazer os 4 dicionários fiscais + `nfse-discriminacao.ts` (`vehicleTypeLabel`) lerem dela. **Nenhuma palavra muda em nenhum documento** até o dono escolher (pergunta 22).
2. **G21 — rótulos fiscais de ouro, com a série, ANTES de qualquer leitor fiscal mudar.** Fixtures (com série, só placa, só chassi, sem nada) → `buildDiscriminacao`, `dps.builder`, `buildBoletoLines`, recibo, `coverageLabels` (`src/utils/quote-tasks.ts:392`), `formatTaskIdentifier` (`src/utils/task.ts:192`), com o **texto exato de hoje**, sobre o objeto que o `select` real de cada serviço carrega. Inclui o DTO de entrada de `elotech.emitNfse({ task: { id, name, serialNumber } })`, que três scripts montam à mão (§6.15).
3. **G5 — `scripts/export-contracts.ts`**: gera `contracts/labels.json` e `contracts/enums.json` (enums e rótulos de tela **e** de nota, grafo e ordem do orçamento como estão hoje, chaves de notificação, campos multipart, `fileContext`). **Faces não entram na Fase A** (a constante é do P04 e ainda não existe no worktree; entram no P11 junto com `'front'`). Testes de **exaustividade** por repositório: todo valor de `IMPLEMENT_TYPE` e `TRUCK_CATEGORY` tem rótulo em **cada** mapa (hoje 9 mapas de tipo e 9 de categoria).
4. **web** (depois do P03): `src/constants/enum-labels.ts` (`TRUCK_CATEGORY_LABELS` `:545`, `IMPLEMENT_TYPE_LABELS` `:558`), `src/utils/changelog-fields.ts:1723-1751`, `src/components/financial/billing/preview/billing-document-previews.tsx:28-57` (cópia dos rótulos fiscais da API) e `src/utils/nfse-discriminacao.ts` passam a vir de uma cópia **gerada** do `contracts/labels.json` (`src/generated/contracts/labels.json`, escrita pelo script da api com `--out`), preservando o perfil de cada tela/documento; teste de exaustividade no vitest.
5. **app** (depois do P02): `lib/features/production/task_enums.dart:104-128`, `lib/shared/domain/task_enums.dart` e `lib/features/financial/nfse_discriminacao.dart:55-80` com o mesmo tratamento (cópia gerada em `lib/generated/contracts/labels.json` ou `.dart`; **sem asset novo** se isso quebrar o patch OTA — na dúvida, gerar `.dart`), e teste de exaustividade. **Não unificar** `kImplementTypeLabels` (tela) com `kNfseImplementTypeLabels` (nota).

## Arquivos que o P05 possui

**api (worktree):** `src/constants/enum-labels.ts`, `src/constants/document-labels.ts` (novo), `src/modules/integrations/nfse/elotech-oxy-nfse.service.ts` (só o dicionário), `src/modules/integrations/nfse/nfse-discriminacao.ts`, `src/modules/integrations/nfse/painter/dps.builder.ts` (só a importação dos rótulos, que passa a ser do perfil `nfsePainter`; o comentário `:522-524` sobre a série fica para o P11), `src/modules/financial/invoice/invoice-generation.service.ts` (só o dicionário), `src/modules/integrations/sicredi/sicredi-boleto.scheduler.ts` (só o dicionário), `scripts/export-contracts.ts` (novo), `contracts/{labels,enums}.json` (gerados), `tests/fiscal-labels-golden.test.ts` e `tests/labels-exhaustive.test.ts` (novos), `tests/nfse-discriminacao.test.ts`, `tests/painter-nfse.test.ts`, `tests/boleto-informativo-cobertura.test.ts` (só acrescentar casos). **web:** os 4 arquivos do item 4, `src/generated/contracts/**`, o teste de exaustividade. **app:** os 3 arquivos do item 5, `lib/generated/contracts/**`, o teste de exaustividade.

## Tipos/schemas compartilhados que toca

O formato de `contracts/*.json` (dono: P05; o P20 e o P24 o consomem); os perfis de rótulo. Lê, sem editar, `src/constants/enums.ts`, `src/utils/task.ts`, `src/utils/quote-tasks.ts`.

## O que NÃO pode tocar

**`package.json` e `scripts/pre-deploy.sh` da api** (donos: P04 nesta fase) — os comandos do P05 rodam como `npx tsx tests/<arquivo>.test.ts` e as entradas `test:fiscal-labels`, `test:labels-exhaustive` e `contracts:export` entram **na integração** (merge de `impl/p05-rotulos`), pelo integrador; `src/constants/implement-faces.ts` (P04); escritores de medida, `task.service.ts`, `portal-*.service.ts`, `schemas/*` (P04/P01); `src/constants/enums.ts` (nenhum valor de enum muda); qualquer leitura da série (continua `task.serialNumber`; os leitores mudam no P28); no web, os arquivos do P03 e a tela de N veículos; no app, os arquivos do P02.

## Testes que o provam

api (no worktree): `npx tsc --noEmit -p tsconfig.json` sem erro novo; `npx tsx tests/fiscal-labels-golden.test.ts` e `tests/labels-exhaustive.test.ts` (novos); `npm run -s test:nfse-discriminacao`, `test:painter-nfse`, `test:boleto-informativo`, `test:nfse-coverage-guard` com o **texto de hoje** (nenhuma asserção existente muda). web: `pnpm exec tsc -b` 0 erros; vitest filtrado (exaustividade + `billing-coverage`, `quote-customers`). app: `flutter analyze` nos arquivos tocados; `flutter test` filtrado (exaustividade + os testes existentes de discriminação da NFS-e).

## Resultado (23/09, P05 executado)

Api: worktree `…/scratchpad/wt/p05/api`, branch **`impl/p05-rotulos`** criada de `feat/portal-do-responsavel` em `d2c61ec8` (o P04 ainda não tinha commit), 5 commits + esta nota, **sem merge na base** (fica para a integração). Web: `feat/portal-do-responsavel`, `fd9a8e75` e `b6f64ccd` sobre `6150bafc`. App: `feat/implemento`, `895e536` sobre `3232757`. Nada empurrado.

| Item | Onde | Estado |
|---|---|---|
| G21 (ouro fiscal) | `tests/fiscal-labels-golden.test.ts` — **escrito e verde sobre o código de antes** (`a7070d71`) | 83 verificações nos construtores REAIS: `ElotechOxyNfseService.buildPayload` (discriminação), `buildServiceDescription` (DPS do aerografista), os dois `buildBoletoLines`, `BudgetReceiptService.generate` (banco e Chromium dublados; lê a linha VEÍCULO do HTML), `coverageLabels`/`coverageSummary`, `formatTaskIdentifier`; fixtures com série, só placa, só chassi, sem nada e a frota de quatro, na forma do `select` de cada serviço; cada palavra de categoria e implemento em cada documento; valor fora do enum passando cru; o DTO `emitNfse({ task: { id, name, serialNumber } })` dos scripts de homologação (a série tem de chegar à nota) |
| Fonte única (D-18) | `src/constants/document-labels.ts` | perfis `screen`, `nfseTask`, `nfsePainter`, `boleto`, `invoice`, `webChangelog`, cada um **por extenso** (nenhum derivado de outro) e congelado; `Record<ENUM, string>` |
| Os 4 dicionários fiscais | `elotech-oxy-nfse.service.ts` (nfseTask), `invoice-generation.service.ts` (invoice), `sicredi-boleto.scheduler.ts` (boleto), `painter/dps.builder.ts` (nfsePainter — era o mapa da TELA); `nfse-discriminacao.ts` usa nfseTask quando ninguém passa mapas | **nenhuma palavra mudou**: ouro 83/83 depois; mutação do perfil de tela não muda nota nenhuma; mutação do perfil do aerografista derruba o ouro |
| Tela | `enum-labels.ts`: `TRUCK_CATEGORY_LABELS`/`IMPLEMENT_TYPE_LABELS` **são** o perfil `screen` | changelog da api, notificações e `quote-text.ts` continuam lendo a tela |
| G5 | `scripts/export-contracts.ts` → `contracts/labels.json` (perfis + 174 mapas `*_LABELS`, cada um com o enum que rotula) e `contracts/enums.json` (199 enums de texto; transições MANUAIS de `Budget.status` derivadas chamando `validateStatusTransition` par a par; `TASK_QUOTE_STATUS_ORDER`; 208 chaves do seed de notificação; 48 rotas multipart lidas do AST dos controllers, todas resolvidas; 54 `fileContext` do `folderMapping`) | determinístico; `--check`, `--out <dir>` (web), `--dart <arquivo>` (app) |
| Exaustividade (api) | `tests/labels-exhaustive.test.ts` | 32/32: todo perfil × 2 enums; tela = `screen`; 171 mapas `X_LABELS` de texto cobrem `X`; nenhum dicionário de categoria/implemento à mão em `src/`; **contrato versionado = o gerado** |
| Web | `src/generated/contracts/{labels,enums}.json` (gerados), `src/constants/document-labels.ts` (novo, importação nomeada de `perfis` — o Vite descarta o resto do JSON), `enum-labels.ts` (screen), `utils/changelog-fields.ts` (webChangelog; `CORRUGATED` legado fica local), `utils/nfse-discriminacao.ts` (nfseTask por padrão), `billing-document-previews.tsx` (NFS-e = nfseTask, boleto = invoice) | `document-labels.test.ts`: perfis × enums do web, enums do web = contrato, tela = screen, mapas `X_LABELS` de texto, palavras de hoje de cada documento e do histórico |
| App | `lib/generated/contracts/labels.dart` (gerado; `.dart`, não asset), `features/production/task_enums.dart` (screen), `shared/domain/task_enums.dart` (`kTaskStatusLabels` do contrato), `features/financial/nfse_discriminacao.dart` (nfseTask) | mesmos nomes, mesma ordem; `kImplementTypeLabels` e `kNfseImplementTypeLabels` **não** unificados; `test/generated/contract_labels_exhaustive_test.dart` |

### Desvios (seguiu-se o código)

1. **Prévia do boleto no web muda de palavra** (`b6f64ccd`): ela usava os rótulos da NFS-e nos DOIS documentos, então mostrava "Toco Isotérmico"/"Carga seca"/"Prancha/Plataforma" num boleto que é registrado com "Isoplastic"/"Carga Seca"/"Carroceria". Passou a ler o perfil `invoice`. Nenhum documento emitido muda; muda só a prévia do boleto, que agora bate com o registrado (é o "prévias fiéis ao servidor" do §6.4). Commit separado para reverter sozinho se o dono preferir.
2. **Perfil `webChangelog` a mais** que os cinco da nota: o histórico do web tem palavras próprias ("VUC (Veículo Urbano de Carga)", "Caminhão") e a nota manda preservar o perfil de cada tela.
3. **O documento do orçamento assinado (`quote-text.ts`) não ganhou perfil próprio**: ele lê a tela, e o arquivo não é do P05. ~~A guarda é o ouro das assinaturas: mudar uma palavra de tela muda o hash e reprova.~~ **Errado (revisão da Fase A, R-B-08):** o hash material guarda o VALOR do enum (`categoryLabel: t.truck?.category`, `signature-envelope.service.ts`), não o rótulo, e o G11 não renderiza `quote-text.ts` — com 'Sider'→'Sider ZZ' no perfil de tela o `test:signature-golden` passou 8/8. A guarda agora é a seção **12b** de `tests/fiscal-labels-golden.test.ts`: cada categoria e cada implemento com a palavra de hoje do documento assinado, escrita por extenso (a mesma mutação reprova). Quando o dono escolher as palavras (pergunta 22), decidir se o documento assinado segue a tela ou congela num perfil `signedQuote`.
4. **`INCLUDE_WHITELIST` não entra no contrato**: é `const` privada de `common/base/include-access-control.ts` (G1, dono P01). Exportá-la é uma linha naquele arquivo; fica para quem fizer o `task_detail_include_test.dart` ler o JSON (P24).
5. **Faces não exportadas** (C2): entram no P11 com `'front'`.
6. **Rótulos novos de tela do §6.4** (`'Caminhão'→'Implemento'`, `IMPLEMENT_CATEGORY_LABELS`, `LAYOUT_STATUS_LABELS` com 5, `REAR_DOOR_LEAVES_LABELS`) **não** entraram: são mudança de texto de tela e valores que ainda não existem — Fase B (o cruzamento põe "rótulo novo de tela" fora da Fase A).
7. `lib/shared/domain/task_enums.dart` (app) não tinha mapa de caminhão; recebeu o mesmo tratamento no que tinha (`kTaskStatusLabels`, idêntico ao `TASK_STATUS_LABELS` da api).
8. O teste de exaustividade (api e web) confere só enum **de texto**: enum numérico (`OP_SIMP_NAC`, `NFSE_ENVIRONMENT`, …) tem o mapa reverso nome↔número e é rotulado pelo número.

### Para a integração (merge de `impl/p05-rotulos`)

- `package.json`: `"test:fiscal-labels": "tsx tests/fiscal-labels-golden.test.ts"`, `"test:labels-exhaustive": "tsx tests/labels-exhaustive.test.ts"`, `"contracts:export": "tsx scripts/export-contracts.ts"`; e as duas primeiras no `scripts/pre-deploy.sh`.
- ⚠️ **O contrato é uma catraca**: `test:labels-exhaustive` reprova se `contracts/{labels,enums}.json` não for o que a api gera. Qualquer mudança em `constants/enums.ts`, `enum-labels.ts`, `document-labels.ts`, seed de notificação, interceptador de arquivo de controller, `folderMapping` ou no grafo do orçamento pede `npx tsx scripts/export-contracts.ts --out ../web/src/generated/contracts --dart ../mobile-flutter/lib/generated/contracts/labels.dart` e o commit dos três. Depois do merge com o P04, rodar `--check` (o P04 não tocou nenhuma dessas fontes até o fim do P05).
- Disjunção conferida: nenhum arquivo em comum entre `d2c61ec8..impl/p05-rotulos` e o que o P04 tinha no checkout principal.

### Testes

- api (worktree): `npx tsc --noEmit -p tsconfig.json` **0** erros antes e depois; o script e os dois testes novos passam num `tsc` à parte (o `tsconfig` da api não inclui `tests/` nem `scripts/`). `tests/fiscal-labels-golden.test.ts` 83/83 (antes e depois); `tests/labels-exhaustive.test.ts` 32/32; `test:nfse-discriminacao` (com o caso novo "sem mapas"), `test:painter-nfse`, `test:boleto-informativo`, `test:nfse-coverage-guard`, `test:nfse-line-scale` verdes sem mudar asserção existente; `test:signature-golden` com `SEM_BANCO=1` 6/6; `export-contracts.ts --check` em dia.
- web: `pnpm exec tsc -b` **0** erros; `vitest run` de `document-labels`, `billing-coverage`, `quote-customers` → 55/55.
- app: `flutter analyze` nos 5 arquivos tocados sem aviso; `flutter test` de `test/generated/`, `nfse_discriminacao`, `billing_approval_plan` (47) e `test/features/production`, `test/shared`, `task_json_contract`, `vehicle_combinations` (127) verdes; arquivos tocados `dart format`-limpos (formatado só o teste novo).

### Integração (mão de merge, 23/09, api)

`git merge --no-ff impl/p05-rotulos` em `feat/portal-do-responsavel` — **sem conflito** (`554a0bc6`). Acrescentadas as entradas prometidas em `package.json`/`scripts/pre-deploy.sh` (`766b0c01`). `npx tsc --noEmit` 0 erros; `test:fiscal-labels` 83/83; `test:labels-exhaustive` 32/32; `export-contracts.ts --check` em dia; `test:nfse-discriminacao`, `test:painter-nfse`, `test:boleto-informativo`, `test:nfse-coverage-guard` verdes sem asserção mudada.

⛔ **`bash scripts/pre-deploy.sh` fica VERMELHO em G6a** (`scripts/guard-residual.sh`, catraca de resíduo "truck", dona P01/P04 — fora do escopo declarado do P05): `document-labels.ts` é arquivo novo com 71 ocorrências (0→71) e os 5 dicionários fiscais/tela cresceram 2 cada (ex.: `invoice-generation.service.ts` 18→20). O padrão do guard (`TRUCK_(?!MANUFACTURER|SPOT)`) casa o próprio identificador do enum `TRUCK_CATEGORY`, que o P05 explicitamente NÃO renomeia nesta fase (nenhum valor de enum muda) — cada perfil (`screen`/`nfseTask`/`nfsePainter`/`boleto`/`invoice`) repete `TRUCK_CATEGORY.<membro>` por construção, então o crescimento é estrutural, não resíduo incidental. `guard-residual.sh --update` recusa sozinho porque a contagem SUBIU (não é regressão para baixar a régua). Isso é análogo à exceção já registrada em `.residual-allowlist` para as chaves de assinatura ("precisa falar truck para sempre" até o enum ser renomeado numa fase futura) — mas decidir se `document-labels.ts` entra no allowlist, ou se a baseline sobe, é do dono do guard (P01/P04), não da mão de merge. **Todo o resto do pre-deploy passou** (20/21 passos verdes); só G6a reprovou.

Bloqueador reportado: worktree e branch `impl/p05-rotulos` **preservados** (não removidos/apagados) até a decisão. Nada empurrado.
