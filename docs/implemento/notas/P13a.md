# P13a — portal grava frente, porta, projeto, e trava medida e série em produção (api, worktree, ∥ P12)

Nota de desenho (preparação da Fase B, 23/09). **Worktree** `/tmp/claude-1000/-home-kennedy-Documents-repositories/b5b01fad-1ec2-4773-82be-b392a838e016/scratchpad/wt/p13a/api`, branch **`impl/p13a`**, criada da base no commit em que o P11b terminou (antes do primeiro commit do P12). Banco **próprio** `ankaa_implemento_p13a` e `node_modules` **próprio** (o P12 promove a M3 e regera o cliente Prisma no meio do par). Preparação exata do worktree em `cruzamento-fase-b.md`. Plano: §5.1 (portal), §6.3 (linhas de escrita), §7.3, D-06, D-09 (`WRITE_VEHICLE_IDENTITY`), DD4, DD5 ("o responsável não edita medida depois de `IN_PRODUCTION`"), pergunta 15 (série também); §9 linha P13a.

## O que mudou em relação ao §9 original (Revisão 3.1)

W4/W5 e as duas unicidades do portal foram para o **P11a** (o gatilho da M1s recusaria a escrita velha até aqui); a **leitura** do portal (frente, porta, projeto: `TASK_BASE_SELECT` e o bloco `implement` da projeção) foi para o **P11b**. Resultado: o P13a não toca `portal-read.service.ts` nem `portal-projection.service.ts`, que neste par são do P12.

## Escopo exato

1. **Frente e porta na escrita do portal** (entrada em cm na borda, metros no banco): `schemas/portal-vehicle-identity.ts` (`medidas.frente`, `portaTraseira: { abertura: 'BIPARTIDA' | 'TRIPARTIDA', varoes: 2|3|4, portinholas: 0..6 }` traduzida para `RearDoorLeaves`; `.strict()` mantido; `implementType` → `type`), `schemas/portal-request.ts` (idem na requisição; `portalMedidasSchema` + `frente`; `medidaParaPrisma` com a regra porta ⇒ altura), `portal-identity.service.ts` (`LADOS_DA_MEDIDA` + `{ chave: 'frente', coluna: 'frontSideMeasureId' }`; **`mexeNoCaminhao` conta toda chave nova** — já custou "200 que não grava"), `portal-request.service.ts` (mapa lado→coluna + frente). Tudo pelo `ImplementMeasureWriter` (P04/P11b).
2. **Documento congelado**: frente e porta **não** são impressas → fora de `VEHICLE_IDENTITY_FIELDS` (sem 409) em `portal-vehicle-identity.ts`/`portal-frozen-document.ts`; a comparação de `category`/`type` continua pelo valor cru (os valores não mudaram).
3. **Multipart**: `implementVinPlate` aceito ao lado de `truckVinPlate` no `FileFieldsInterceptor` de `portal-identity.controller.ts` (o contexto já existe desde o P11a).
4. **Projeto do implemento pelo portal**: `POST /cliente/me/veiculos/:taskId/projeto` (multipart `implementProject`, PDF ou imagem; `WRITE_VEHICLE_IDENTITY`; escopo comercial; 404 fora do escopo) grava em `Implement.projectFiles` com o contexto `implementProjectFiles` (P11b).
5. **Trava em produção** (DD5, pergunta 15): com a tarefa em `IN_PRODUCTION` ou depois, o PATCH de identificação recusa medida, porta **e série** com 409 nomeado ("O veículo já está em produção: fale com a Ankaa para corrigir"); placa/chassi seguem as regras de hoje.
6. **Contrato**: `docs/PORTAL-CONTRATO.md` (e `docs/PORTAL-DO-RESPONSAVEL.md` onde citar medidas) com `medidas.frente`, `portaTraseira`, a rota do projeto e a trava; o integrador acrescenta o trecho que o P12 deixar na nota dele (arte do orçamento).

## Arquivos que o P13a possui neste par

`src/modules/people/portal/{portal-identity.service,portal-identity.controller,portal-identity.module,portal-request.service,portal-request.controller,portal-request.module,portal-vehicle-identity,portal-frozen-document}.ts`, `src/schemas/portal-request.ts`, `src/schemas/portal-vehicle-identity.ts`, `tests/{portal-identificacao,portal-requisicao,portal-cliente-boot}.test.ts`, `tests/e2e-portal/**` (cenário 01), `docs/PORTAL-CONTRATO.md`, `docs/PORTAL-DO-RESPONSAVEL.md`. Gerados que ele **pode** regerar no próprio ramo se a régua dele pedir (e que a integração regera de novo): `contracts/{labels,enums}.json` (o campo multipart novo entra no contrato).

## O que NÃO toca

`prisma/**` (nenhuma migração, nenhum `generate` fora do próprio `node_modules`; se precisar de coluna nova: **pare** e reporte — o carimbo reservado `20260924120295`–`120299` só entra pela integração); `package.json` e `scripts/pre-deploy.sh` (roda os testes com `npx tsx …`; o integrador registra as entradas); `portal-read.service.ts`, `portal-projection.service.ts`, `portal-notification.service.ts`, `portal-capabilities.ts`, `src/modules/common/**`, `src/modules/production/**`, `.residual-baseline.json` (não rodar `--update`), `contracts/file-contexts.json`; web e app.

## Testes que o provam

`test:portal-identificacao` ("PATCH só com `medidas.frente` grava"; porta fora da faixa → 400; `portaTraseira` sem medida grava; trava em `IN_PRODUCTION` para medida, porta e série; frente não gera 409 de documento congelado), `test:portal-requisicao` (requisição com 4 faces e porta; implemento nasce com série, `spot: null`), `test:portal-cliente:boot` (a rota do projeto no grafo), e2e-portal 01 (+ frente, + porta, + projeto) quando a pilha dev estiver no ar; a régua **no worktree** (`source` do env do P13a) verde.
