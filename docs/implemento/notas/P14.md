# P14 — a máquina do orçamento, a cobrança só depois de assinado, "Assinado fora do sistema" e o pedido de compra (api, checkout principal, ∥ P13b)

Nota de desenho (preparação da Fase B, 23/09). **Checkout principal**, commits na base, em paralelo com o P13b (worktree `impl/p13b`, que só nasce **depois do commit zero** abaixo). Plano: §2A inteiro, §5.1/§5.5 (orçamento, cobrança, DD11, DD12), §6.2 itens 17 (valor), 18 e 32, §6.14 (API), §6.15 (orçamento), D-26..D-36, **DD7, DD8, DD11, DD12**; §8 G26–G29, G31, G33 (coleta), G34, G35, G36; §9 linha P14.

## Commit zero (antes de o worktree do P13b nascer; sem mudar comportamento)

1. **Promove a M3o-a** (`20260930120300_orcamento_eixo_da_assinatura`): `BudgetSignatureStatus` (com `SIGNED_OFFLINE`), `BudgetValueApprovalSource`, `BudgetValueApproval`, `BudgetOfflineSignature`, `Budget.signatureStatus` (+ índice) e o eixo calculado do envelope vigente (`WAIVED` do legado, triagem). Blocos do alvo em `schema.prisma`; espelhos TS `BUDGET_SIGNATURE_STATUS`, `BUDGET_VALUE_APPROVAL_SOURCE` e rótulos (inclusive **"Assinada fora do sistema"**); contratos regerados + cópias no web e no app.
2. **Rename D-35 inteiro**: `PORTAL_CAPABILITY.PRE_APPROVE → APPROVE_VALUE` (mesmos papéis; rótulo "aprovar o valor ou recusar orçamento"), `PUT …/pre-aprovar → …/aprovar-valor`, `canPreApprove → canApproveValue`, `waitingOnMe.preApproval → waitingOnMe.valueApproval`, nos arquivos do portal e nos testes (`portal-decisao`, `portal-cliente-boot`, `portal-recorte`) — só nomes; o destino continua `PRE_APPROVED` até o fim do pacote.
3. Régua verde → commit → o P13b cria o worktree **deste** commit.

## Escopo exato (depois do commit zero)

1. **Eixo do valor (§2A.3/§2A.5)**: grafo em duas tabelas (manuais × sistema) exportado ao G5; atos `PUT /budgets/:id/send-to-customer`, `…/withdraw-from-customer`, `PUT/DELETE /budgets/:id/value-approval` (nota/motivo obrigatórios); `PUT /:id/status` delegando aos atos com `validateQuoteStatusChangeRole` (X7); `budgetApprove` = "aprovar valor" (sem portão de layout); `LEGACY_APP` (D-36); nascimento decidido no servidor (D-34); `BudgetValueApproval` como **registro** (sem hash, DD8), fechado quando o orçamento sai de `APPROVED` — o auto-revert e o "fixar Aprovado" **como hoje** (DD8), nos dois gêmeos (`budget.service.ts`, `task.service.ts:808-820,11060-11086`); `service-order.service.ts:209-230` não ressuscita `APPROVED` sem aprovação vigente; X1–X9.
2. **Eixo da assinatura (§2A.4)**: um escritor, **na transação do envelope** (emissão, grupo 0, conclusão, recusa, vencimento, invalidação, cancelamento); a emissão **não** escreve `Budget.status`; `replayCompletion` reconhece "grupo 0 completo e eixo em `AWAITING_CUSTOMER`" (X9); conclusão de coleta legada aprova com `BudgetValueApproval{SIGNATURE}`; `tolerateArtworkAfterSeal` só em `COMPLETED` (G33, lado coleta); `signature-expiry.scheduler.ts`.
3. **Emissão (§2A.7, D-29)**: `assertEmissionReady(quoteId, tx?)` nos três lugares (preflight, `createEnvelope`, transação); E2 reusa `quoteArtworkOf` (P12); `gates` estruturado ao lado de `blockers: string[]` **intocado** (G27); `emission { ready, blockers[] }` no detalhe do orçamento. Exporta `emissionOf(quote)` para o integrador ligar no portal.
4. **Cobrança (DD7 + DD11)**: `internalApprove` exige `status = APPROVED` ∧ `isBillableSignatureStatus(signatureStatus)` = `{SIGNED, SIGNED_OFFLINE, WAIVED}` — **um** predicado em `src/utils/budget-signature.ts`, usado também por `billable` na leitura e pelo filtro; 400 com a frase da DD7; liquidar/reverter não passam pelo portão; filtro do financeiro positivo (X6); `billing.controller.ts` lê `signatureStatuses`.
5. **DD11 — "Assinado fora do sistema"**: `POST /budgets/:id/offline-signature` (ADMIN/COMMERCIAL; multipart `offlineSignatureFile` 1 arquivo PDF/imagem + `note` obrigatória + `signedAt?`); guarda E1 ∧ E3 e eixo em `NOT_ISSUED`/`REFUSED`/`EXPIRED`/`INVALIDATED` (409 nos outros); transação: `BudgetOfflineSignature` + eixo `SIGNED_OFFLINE` + `ChangeLog` (campo `signatureStatus`, motivo = nota, `metadata { offlineSignatureId, fileId }`) + `task_quote.signed` ao financeiro; ao sair de `APPROVED`: eixo → `INVALIDATED`, registro fechado. Contexto de arquivo `budgetOfflineSignature` (`files-storage.service.ts`, `contracts/file-contexts.json`) e `'BudgetOfflineSignature.fileId'` em `file-reference.service.ts` (G10). E2 **não** é exigido (a arte trava a produção pela DD3, não a cobrança) — confirmar com o dono sem travar o pacote.
6. **DD12 — pedido de compra**: `orderNumberRequirement({ roles, tasks })` em `order-number-gate.ts` (sujeito = tem `PURCHASING`; escopo = `orderNumberScope`; tem número = `customerOrderNumber` não vazio ∨ `purchaseOrderId`); `signWithOtp` e `signByPortalSession` usam o mesmo predicado, recebem `orderNumbers[]` (`portalSignSchema` ganha o campo), resolvem com `resolveOrderNumberSubmission` (400) **antes** do ato e gravam com `writeInformedOrderNumbers`; a leitura das duas cerimônias devolve `orderNumber: { required, maxLength, vehicles }` (sai `pedidoDeCompra` da leitura do portal — nunca foi a produção; o web do portal se ajusta no P23). **Apaga** `purchase-order-gate.ts` e ajusta quem o importa (`tests/portal-assinatura-compras.test.ts`, `tests/portal-identificacao.test.ts:54,514`, `tests/e2e-portal/cenarios/03-portao-compras.ts`, o comentário de `portal-capabilities.ts:129` fica para o P13b).
7. **Portal, lado escrita**: `portal-decision.*` (`APPROVE_VALUE: IN_NEGOTIATION → APPROVED` com `BudgetValueApproval{PORTAL}`; recusa → `PENDING`; para de fabricar `BudgetRequest`; o aviso ao comercial diz o que falta).
8. **Fim do pacote: promove a M3o-b** (`20260930120350_orcamento_valor_aprovado`): `BudgetStatus` sem `PRE_APPROVED`, `queueRank`, `statusOrder` default 3, `BudgetValueApproval{MIGRATED}`; espelhos TS (`src/constants/enums.ts`, `sortOrders.ts`), listas positivas derivadas do enum (G31), `orcamento-sem-os-negociacao` com o rótulo "Aguardando aprovação do cliente" (pergunta 12); notificações novas no seed (`task_quote.value_approved`, `task_quote.ready_for_signature`); regra de Atenção `budget.ready-to-emit`.

## Arquivos que o P14 possui neste par

`prisma/schema.prisma` + as duas pastas promovidas; `src/modules/production/budget/**`, `src/modules/financial/billing/{billing.controller,billing.service}.ts`, `src/modules/financial/invoice/invoice-analytics.service.ts`, `src/modules/common/signature/**` (inclusive `order-number-gate.ts`, `purchase-order-gate.ts`, `portal-signature.controller.ts`, `signature-expiry.scheduler.ts`), `src/modules/people/portal/portal-decision*.ts`, `src/modules/common/attention/attention.service.ts`, `dashboard-prisma.repository.ts`, `service-order.service.ts`, `task.service.ts` (só o gêmeo do auto-revert), `schemas/{budget,task,signature}.ts`, `src/utils/budget-signature.ts` (novo), `src/constants/{enums,enum-labels,sortOrders}.ts`, `src/modules/common/file/**`, `contracts/**` + cópias web/app, `prisma/scripts/seed-notification-configs.ts`, `tests/{budget-state-machine,budget-value-approval,emission-gate,offline-signature,portal-decisao,portal-assinatura-compras,orcamento-sem-os-negociacao,billing-status}.test.ts`, as linhas do gate em `tests/portal-identificacao.test.ts`, `tests/e2e-portal/cenarios/03*`, `package.json`, `scripts/pre-deploy.sh`, `.residual-baseline.json`, `.serial-allowlist`.

## O que NÃO toca (depois do commit zero)

`portal-read.service.ts`, `portal-projection.service.ts`, `portal-capabilities.ts`, `portal-artwork.*`, `portal-notification.service.ts`, `scripts/seed-portal-demo.ts`, `tests/{portal-recorte,portal-cliente-boot}.test.ts`, `docs/PORTAL-CONTRATO.md` (P13b). O que o portal precisa ler do P14 (`emissionOf`, a frase da DD7, o contrato do `orderNumber`) vai na seção "Resultado" desta nota; o integrador liga o `emission` do portal.

## Testes que o provam

`budget-state-machine.test.ts` (§2A.5 inteira, manuais × sistema; DD8: auto-revert sem status fixado, fixado mantém `APPROVED`, a aprovação vigente fecha ao sair de `APPROVED`), `budget-value-approval.test.ts`, `emission-gate.test.ts` (**G28**: mesmo cenário pelos três caminhos; **G27**), **G26** (grafo × espelhos, pelo G5), **G29** (eixo = função do envelope vigente, salvo `WAIVED` e `SIGNED_OFFLINE` com registro vigente), **G31**, **G33** (coleta), **G34** (os três caminhos do `internalApprove`: `NOT_ISSUED`/`AWAITING_*`/`REFUSED`/`EXPIRED`/`INVALIDATED` → 400; `SIGNED`/`SIGNED_OFFLINE`/`WAIVED` → passa), `offline-signature.test.ts` (sem nota/sem anexo → 400; com coleta viva → 409; transação; `ChangeLog` com a nota; sair de `APPROVED` invalida e fecha), **G35** (tabela-verdade pelos dois atos + varredura de fonte), `portal-decisao` e `portal-assinatura-compras` reescritos, `test:portal-cliente:boot`, ensaio da R-B com tudo aplicado, **G36 `--final` vazio** na integração; régua inteira.

## Resultado (25/09, na nuvem)

Feito no checkout principal, sobre o commit zero (`d6e4215`).

- **Eixo do valor (§2A.3/§2A.5):** grafo em duas tabelas em `src/modules/production/budget/budget-transitions.ts` (`BUDGET_MANUAL_TRANSITIONS` × `BUDGET_SYSTEM_TRANSITIONS`), exportadas no contrato (`orcamento.transicoesManuais` / `transicoesDoSistema`, G26). Atos: `PUT /budgets/:id/send-to-customer`, `…/withdraw-from-customer`, `PUT/DELETE /budgets/:id/value-approval` (nota/motivo obrigatórios, zod `.strict()`), `PUT /:id/status` delegando aos atos com `validateQuoteStatusChangeRole` (X7); `budget-approve` com nota = `ON_BEHALF`, sem nota = `LEGACY_APP` (D-36). A gravação genérica NÃO chega mais a `APPROVED` (400 "aprovar o valor é um ato com nota"; fixar o `APPROVED` atual continua). Nascimento no servidor (D-34) nos três caminhos (`BudgetService.create` e os dois do repositório da tarefa). `BudgetValueApproval` como registro, com um reconciliador só (`src/utils/budget-value-approval.ts`: `syncValueApprovalWithStatus`), chamado no `update()` do orçamento e por todos os escritores de status fora dele (cancelamento pela tarefa e pelos dois ramos da O.S., união, desmonte); a reativação pela O.S. comercial não ressuscita `APPROVED`/`SIGNED` (volta a `PENDING`). A escrita aninhada da tarefa já recusava mudar status de orçamento existente (400) — o "gêmeo" do auto-revert em `task.service.ts` ficou sem efeito prático.
- **Eixo da assinatura (§2A.4):** escritor único `SignatureEnvelopeService.writeSignatureAxis(tx, …)` (condicional ao valor lido; trilha `signatureStatus` no ChangeLog na mesma transação), chamado na emissão (`AWAITING_CUSTOMER`; a emissão não escreve mais `Budget.status` — X1), no grupo 0 (`AWAITING_ANKAA`, agora com `await`), na conclusão (`SIGNED`, na transação do artefato), na recusa (`REFUSED`), no vencimento (`EXPIRED`, a varredura virou uma transação), na invalidação (`INVALIDATED`) e no cancelamento (`NOT_ISSUED`). `replayCompletion` repara o X9 (grupo 0 completo com o eixo parado) e o eixo `SIGNED` da coleta concluída. O gancho de conclusão virou `BudgetService.onSignatureCompleted`: coleta LEGADA (sobre `PENDING`) aprova com `BudgetValueApproval{SIGNATURE}`; coleta nova só avisa (`task_quote.signed`). `markSigned` não escreve mais `SIGNED`; `markInvalidatedBySignature` não rebaixa mais o valor (só o `SIGNED` legado).
- **Emissão (§2A.7, D-29):** `src/utils/emission-gate.ts` — `emissionGatesOf` (E1–E6 com `detail`), `emissionOf`, `assertEmissionReady(client, quoteId)`, chamados no preflight (`gates` ao lado de `blockers: string[]` intocado, G27), no `createEnvelope` antes do render e dentro da transação. E7 (recortes) continua no `createEnvelope`. `emission` e `valueApproval` no detalhe do orçamento (`GET /budgets/:id`). "Pronto para emitir": `task_quote.ready_for_signature` quando o valor chega com as artes aprovadas, ou quando a última arte chega (ouvinte de `artwork.approved` no `BudgetModule`).
- **Cobrança (DD7):** `src/utils/budget-signature.ts` (`isBillableSignatureStatus`, `isQuoteBillable`, `BILLABLE_QUOTE_WHERE`, a frase da DD7); portão em `internalApprove`; `billable` na leitura do repositório; filtro do financeiro positivo (X6); `GET /billings?signatureStatuses=`; `signatureStatus` no detalhe e na lista do faturamento.
- **DD11:** `POST /budgets/:id/offline-signature` (multipart `offlineSignatureFile`, `note`, `signedAt?`), contexto `budgetOfflineSignature` (pasta `Orcamentos/Assinaturas`), G10 com contexto. Não exige E2 (pendência 4 do ESTADO segue com o dono).
- **DD12:** `orderNumberRequirement` em `order-number-gate.ts` (sujeito = TEM `PURCHASING`; escopo; tem número = número ∨ `purchaseOrderId`); as duas cerimônias usam o predicado, o portal recebe `orderNumbers[]` e resolve com 400 antes do ato, grava com `writeInformedOrderNumbers`; a leitura do portal devolve `orderNumber` (sai `pedidoDeCompra`). `purchase-order-gate.ts` APAGADO.
- **Portal, escrita:** `APPROVE_VALUE: IN_NEGOTIATION → APPROVED` com `BudgetValueApproval{PORTAL}` (contato como ator, `userId` nulo); recusa → `PENDING`; sem fabricar `BudgetRequest`; o aviso ao comercial diz o que falta para emitir.
- **M3o-b promovida** (`20260930120350_orcamento_valor_aprovado`): `BudgetStatus` sem `PRE_APPROVED`, `statusOrder @default(3)`, `queueRank` novo (objetos pós-push regerados), espelhos TS, `budgetStatusSchema = z.nativeEnum` (G31), rótulos ("Pendente", "Aguardando aprovação do cliente", "Assinado (legado)"); seed com `task_quote.value_approved` e `task_quote.ready_for_signature`; regra de Atenção `budget.ready-to-emit` (o gêmeo do web fica para o P22). G36 = 0.
- **Testes:** `budget-state-machine` (45), `emission-gate` (48), `offline-signature` (23), `portal-decisao` e `portal-assinatura-compras` (G35) reescritos, `orcamento-sem-os-negociacao`, `signature-refusal` e `portal-identificacao` ajustados, `e2e-portal/cenarios/03` reescrito para a DD12 (não roda na nuvem).

### Para o contrato do portal (P13b/integração)

- `PUT /cliente/me/orcamentos/:id/aprovar-valor` → `APPROVED` (Modelo C) com `BudgetValueApproval{PORTAL}`; `PUT …/recusar` → `PENDING`. `decision: 'APPROVE_VALUE' | 'REFUSE'`.
- `GET /cliente/me/assinaturas`: cada item traz `orderNumber: { required, maxLength, vehicles: [{ taskId, label, value, hasNumber }] } | null` (null = não tem Compras). `POST …/:signerId/assinar` aceita `orderNumbers: [{ taskId, value }]`; faltando → 400 "Informe o nº do pedido de compra para assinar.".
- `emission: { ready, blockers: [{ code, message }] }` sai de `emissionOf(client, quoteId)` (`src/utils/emission-gate.ts`).

### Achado fora do escopo

`PUT /budgets/:id/status {CANCELLED}` responde "Orçamento cancelado com sucesso." e NÃO cancela quando ainda há veículo ativo (`cancelForTaskCancellation` sai em silêncio). Anterior ao P14.
