# P01 — Portões de máquina (api, **sozinho**)

Nota de desenho (P00, 23/09). Base: `feat/portal-do-responsavel` em `cd61b7d1` (merge da `main` do deploy de 23/09). Plano: §8 (G0, G1, G4, G6a/b, G7, G9, G10, G11, G14, G25), §5.3, §9 linha P01.

## Escopo exato

Criar as guardas que o resto do rework vai usar, **sem mudar o que os clientes atuais recebem**:

1. **G0 — tipo real.** Script `typecheck` (`tsc -p tsconfig.json --noEmit`) e um `tsconfig.check.json` que inclua `tests/` e `scripts/`; `noEmitOnError: true` no `tsconfig.build.json`. ⚠️ Hoje o `tsc` da api tem **3 erros** (pré-existentes, os mesmos antes e depois do merge): `src/modules/production/budget/budget-receipt.service.ts:184-185` (`document` sem `lib: dom`, código que roda dentro do navegador do puppeteer) e `src/scripts/audit-painter-nfse-setup.ts:21` (`@xmldom/xmldom` não instalado). Com `noEmitOnError` o build quebra: corrigir sem mudar comportamento (tipar o callback do `page.evaluate` / `declare const document`, e tirar o script de auditoria do build ou do `include`), **sem** instalar dependência.
2. **G1 — validador de consulta derivado do DMMF** (`src/modules/common/query/dmmf-query-validator.ts`) + `DEPRECATED_QUERY_KEYS` (única tabela, com data de expiração; já com `BudgetPayer.responsible → descarta` e `Budget.task → tasks`). Plugar em todas as rotas que aceitam `include/select/where/orderBy` (começando por `task.controller.ts`). **Na Fase A:** chave desconhecida que **hoje dá 500** (passthrough até o Prisma) passa a **400 nomeado**; chave que hoje é **descartada calada** por schema enumerado fica em **modo relatório** (loga e conta, não recusa) até o censo (G3, P06) provar que nenhum cliente a manda. É o que mantém a regra "nenhuma mudança de comportamento" e ainda assim tira o 500.
3. **G4 base (API)** — `tests/query-contract.test.ts` + pasta `contracts/queries/`: cada forma passa por zod da rota → G1 → `prisma.<model>.findFirst({...forma, take: 1})` em transação revertida no clone; cruza whitelist × zod × DMMF. Fixtures iniciais: as 4 formas do **`AnkaaAero`** (`contracts/queries/ankaa-aero.json`, escritas à mão a partir de `mobile-flutter/AnkaaAero/AnkaaAero/Networking/APIClient.swift:63-158` — terceiro cliente achado no P00) e sementes à mão das constantes de include mais usadas do web e do app. O extrator do web é do P03; o do app, do P24.
4. **G6a/G6b** — `scripts/guard-residual.sh` (comando **sem `-i`**, base gravada da primeira execução em `.residual-baseline.json`, catraca que só cai) e o `rg` de identificador colado a texto de tela.
5. **G7 base** — `tests/sector-field-matrix.test.ts` (cada setor com o corpo real que o formulário dele manda; proibido testar só como ADMIN).
6. **G9, G10, G11 base, G25** — `tests/notification-keys.test.ts`, `tests/file-references.test.ts`, `tests/signature-golden-hashes.test.ts` (envelopes do **clone**, anonimizados; os 30 COMPLETED de produção entram no P30 ou por exportação somente leitura autorizada pelo dono), `prisma/sql/objetos-pos-push.sql`.
7. **G14** — `scripts/pre-deploy.sh` (roda G0 + guardas + `test:portal-cliente:boot`; e2e fica opcional por flag). Desenhado para receber **uma linha por pacote** (P04 e P05 acrescentam a sua).
8. Limpezas do plano: apagar `src/schemas/task.ts:634-1094` (sem importador); corrigir os fantasmas que o G1 acusar (`cutRequest`, `cutPlan`, `bonifications`, `customer.tasks.include.*`, `SELECT_WHITELIST.Task.truckId`, `File.tasksLayouts`); parar de embrulhar `PrismaClientValidationError` em 500; mensagem de P2002 para `serialNumber` em `src/common/filters/global-exception.filter.ts` (o caminho real é `src/common/…`, não `src/modules/common/…` como o plano dizia).

## Arquivos que o P01 possui

`tsconfig.json`, `tsconfig.build.json`, novo `tsconfig.check.json`, `package.json` (scripts), `scripts/pre-deploy.sh` e `scripts/guard-residual.sh` (novos), `.residual-allowlist`, `.residual-baseline.json`, `prisma/sql/objetos-pos-push.sql`, `src/modules/common/query/**` (novo), `src/modules/common/base/include-access-control.ts`, `src/modules/common/base/include-mapper.helper.ts`, `src/common/filters/global-exception.filter.ts`, `src/schemas/{task,customer,file}.ts` (só as partes de consulta e o bloco morto), `src/modules/production/task/task.controller.ts` (plugar o G1), `contracts/queries/**`, `tests/{query-contract,sector-field-matrix,notification-keys,file-references,signature-golden-hashes}.test.ts`, e os dois arquivos com erro de tipo do item 1.

## Tipos/schemas compartilhados que toca

`DEPRECATED_QUERY_KEYS` e o contrato do G1 (todos os pacotes depois usam); `INCLUDE_WHITELIST`; os schemas de consulta de `schemas/task.ts`. Não cria enum, rótulo, face nem tipo de domínio.

## O que NÃO pode tocar

Schema Prisma e migrações; escritores de medida (P04); rótulos e dicionários fiscais (P05); qualquer serviço de orçamento, assinatura ou faturamento; web e app. Nenhum `sed` global.

## Testes que o provam

`npx tsc --noEmit -p tsconfig.json` com **0** erros (hoje 3); `npm run typecheck`; os novos `query-contract`, `sector-field-matrix`, `notification-keys`, `file-references`, `signature-golden-hashes`; `test:portal-cliente:boot`; e, para provar que nada mudou para os clientes, os testes existentes do P00 (`quote-diff`, `quote-merge`, `portal-recorte`, `portal-decisao`, `portal-requisicao`, `billing-coverage`, `nfse-discriminacao`, `billing-lens`, `layout-per-vehicle`). `scripts/pre-deploy.sh` verde.

---

## Resultado (23/09, P01 executado)

Branch `feat/portal-do-responsavel`, de `4168be37` a `3bb71eb7` (14 commits, nada empurrado). `scripts/pre-deploy.sh` **verde** (18 passos, 83 s): G0, G6, G1+G4, G7, G9, G10, G11, `quote-diff`, `quote-merge`, `portal-recorte`, `portal-decisao`, `portal-requisicao`, `billing-coverage`, `nfse-discriminacao`, `billing-lens`, `orcamento-sem-os-negociacao`, `layout-per-vehicle`, `test:portal-cliente:boot`.

### O que existe agora (e onde)

| Guarda | Onde | Número de hoje |
|---|---|---|
| G0 | `tsconfig.build.json` `noEmitOnError: true`; `npm run typecheck` (src, **0**); `npm run typecheck:full` = `scripts/typecheck-ratchet.ts` sobre `tsconfig.check.json` (src + tests + scripts) contra `.typecheck-baseline.json` | src **0** (eram 3); tests+scripts **138** erros na catraca, por assinatura arquivo+código+mensagem (sem linha); qualquer erro novo, ou qualquer erro em src/, reprova; `--update` só desce |
| G1 | `src/modules/common/query/{dmmf-query-validator,deprecated-query-keys,query-shape.guard,query-key-telemetry,prisma-validation-error}.ts`; pipe `ZodQueryValidationPipe(schema, { queryModel })` | plugado em **todas** as rotas de `/tasks` com `@Query` (18 `taskQuerySchema` + 2 `taskGetManySchema` + o `PUT /:id`) |
| G4 base | `tests/query-contract.test.ts` + `contracts/queries/{ankaa-aero,web.seed,flutter.seed,negativas}.json` | 46 verificações; 4 formas AnkaaAero + 6 web + 4 app passam no zod → G1 → Prisma (transação desfeita); 5 negativas recusadas pelo G1 **e** pelo Prisma |
| G6a/G6b | `scripts/guard-residual.sh` + `.residual-baseline.json` + `.residual-allowlist` | G6a **1.320** ocorrências em **104** arquivos (`rg --no-config -P --count-matches`, sem `-i`); G6b **42** em **6** arquivos |
| G7 base | `tests/sector-field-matrix.test.ts` | 39 verificações; 8 corpos de edição e 4 de criação, nenhum ADMIN |
| G9 | `tests/notification-keys.test.ts` | 140 chamadas, 133 chaves distintas, 208 no seed |
| G10 | `tests/file-references.test.ts` (+ `contracts/file-contexts.json`) | catálogo lido do banco; 9 FKs sem linha já existiam (lista que só encolhe) |
| G11 base | `scripts/export-signature-golden.ts` → `contracts/signature-golden/envelopes.json`; `tests/signature-golden-hashes.test.ts` | clone: **35** envelopes, 35 reproduzem `quoteSnapshotSha256`; **23** com `quoteTermsSha256`, os 23 casam (**8 na v6, 15 na v7**); 12 sem hash de termos (snapshot v1); **0 PER_VEHICLE** no clone → 2 sintéticos (cobertura uniforme e não uniforme) |
| G14 | `scripts/pre-deploy.sh` (`npm run pre-deploy`, `--e2e`, `--sem-banco`) | um bloco de linhas por pacote (P04 e P05 acrescentam o seu) |
| G25 | `scripts/gen-objetos-pos-push.ts` → `prisma/sql/objetos-pos-push.sql` | 2 extensões, 8 funções, 171 colunas geradas, 5 gatilhos, 17 índices, 21 CHECKs; aplicado **duas vezes** no clone numa transação desfeita, sem erro |

### Correções de comportamento feitas (todas "500 → 400/200", nenhuma forma real muda)

- Chave de consulta que o modelo não tem, nas rotas de `/tasks`: 400 `Chave de include desconhecida: Customer.nfe (em include.customer.include.nfe)` em vez do 500.
- `task.service.ts`: os 9 `catch` que embrulhavam em `InternalServerErrorException` relançam `PrismaClientValidationError` como 400 nomeado; o `nomeDaRelacaoDesconhecida` do `findById` virou `unknownPrismaKeyOf` no módulo comum. Filtro global: 400 de validação nomeia a chave; P2002 de `serialNumber` → "Este número de série já está em uso.".
- Fantasmas removidos: tarefa (`cutRequest`, `cutPlan`, `bonifications`, `invoiceIds/receiptIds/bankSlipIds` no where, e 7 escalares inexistentes nos selects aninhados), cliente (`tasks.include.{budget,nfe,files,airbrushing}`, `phone`, `mobilePhone`), arquivo (`tasksLayouts`, `airbrushingLayouts`, `externalOperationBudgets`), aerografia (`task.include.{files,bonifications,airbrushing}` e as listas de relações de File/Layout copiadas de modelo antigo), whitelist (`Task.updatedBy`, `SELECT Task.truckId`, `Task.updatedBy`, `User.role`, `User.ledSectorId`, `User.ppeSizeId`).
- Filtros `isOrphaned`/`hasRelations` de arquivo davam 500 **sempre** (citavam `tasksLayouts` e `externalOperationBudgets`): agora usam `layouts: { is/isNot: null }`.
- Legado traduzido pelo G1 nas rotas de tarefa: `Budget.task → tasks` e `BudgetPayer.responsible` descartado em qualquer profundidade (antes: 500 se passasse pelo `z.record`).
- Código morto apagado: `schemas/task.ts:634-1094` (confirmado sem importador, nem por `export *`).

### Desvios do plano (seguiu-se o código)

1. **G0**: o `tsc` de src tinha os 3 erros previstos; `tests/`+`scripts/` têm **138** (fora os ~2.400 dos `*.spec.ts` de jest sem jest, que continuam excluídos como já estavam). Em vez de zerar, catraca por assinatura (instrução do pacote). `audit-painter-nfse-setup.ts` saiu do build e dos dois typechecks (importa `@xmldom/xmldom`, que não é dependência declarada — o pnpm não o içou e ele não resolve nem em runtime). ⚠️ **`tests/painter-nfse.test.ts` importa o mesmo módulo** e por isso não roda hoje (fica na catraca como `TS2307`) — o P05 lista `painter-nfse` entre os seus testes: precisa declarar a dependência ou trocar o import.
2. **G1 plugado só em `/tasks`** (o arquivo da nota). As demais rotas ficam como eram até o dono de cada uma plugar (`{ queryModel }` no pipe); o G4 já passa as formas de orçamento e aerografia pelo G1 para provar que o plug não mudará nada.
3. **G1 na Fase A**: recusa só o que **chegaria ao Prisma** (a consulta depois do zod). O que o zod descarta calado fica em modo relatório (log + contador em memória, por modelo+caminho — a rota não é visível dentro do pipe; o censo por rota × versão do app é o G3 do P06). Achados do relatório já nas fixtures: `include.cuts` (o web pede, `taskIncludeSchema` não declara), `orderBy.truck.plate` (painel do web), `Airbrushing.layouts.include.file` (AnkaaAero). **Não** acrescentei `cuts` ao schema: seria dado novo chegando ao web na Fase A.
4. **`DEPRECATED_QUERY_KEYS`** ganhou a coluna `handledBy`: além das 2 linhas do plano (traduzidas pelo G1), registra o legado que o repositório de orçamento já traduzia (`Budget.task`/`taskId` no where e no orderBy), para a tabela ser a única lista. As chaves calculadas pelo repositório (`Task.orderBy.currentInstallmentDueDate`) moram no mesmo arquivo, em `COMPUTED_QUERY_KEYS`. Janelas vencem em 2027-03-31 (o teste de contrato reprova depois).
5. **Fantasma que ficou**: `tasks.include.airbrushing` em `schemas/budget.ts` (arquivo do P14; a relação é `airbrushings`) — em `FANTASMAS_CONHECIDOS` do teste de contrato, lista que só encolhe. `schemas/airbrushing.ts` não estava na lista de arquivos da nota, mas nenhum pacote da Fase A o possui; foi corrigido aqui.
6. **Forma real do web quebrada hoje**: `task-export.tsx:216-220` pede `serviceOrders.include.service` (não existe) → era 500, agora 400 nomeado; está em `negativas.json`. O conserto do lado do web é o A12 do P03.
7. **G6b** conta só dentro de literal de string (o padrão cru casava comentário); base 42.
8. **G7**: os corpos são representativos, escritos à mão a partir da origem citada; o P11 os troca pelos exatos e acrescenta `implement`/série.
9. **G9 achou**: `task.field.truck.vinPlateId` é emitida pelo tracker e **não tem configuração no seed** — trocar a foto da plaqueta nunca notificou ninguém. Registrado (lista que só encolhe); pôr no seed muda quem recebe notificação → **decisão do dono** (reseed do P26). 11 chamadas com chave montada por função ficam listadas com o motivo.
10. **G10**: as tabelas `INBOUND_REFERENCES`/`OUTBOUND_REFERENCES` passaram a ser `export` (só isso em `file-reference.service.ts`). 9 FKs sem linha pré-existentes: NFS-e do aerografista (pdf/xml), análise de pintura (2), documentos do envelope (2), adendo, admissão, e `BudgetLayoutTask.fileId` (layout por veículo, sai no P12).
11. **G25**: `tests/task-match-integration.test.ts:137` ainda grava `serialNumberNormalized` à mão. Com o SQL aplicado depois do `db push` isso passa a ser erro (coluna gerada); não mexi porque o teste exige um banco de `db push` próprio e a Fase A proíbe `db push` local — fica para o P10, que sobe o banco de QA por migração.
12. **Pegadinha registrada no script**: `TMPDIR` com caminho longo derruba o tsx (`listen EINVAL` no pipe de IPC) — os logs do pre-deploy usam `PRE_DEPLOY_LOG_DIR`, nunca `TMPDIR`.
